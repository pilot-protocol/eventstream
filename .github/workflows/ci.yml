name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout this repo
        uses: actions/checkout@v4
        with:
          path: eventstream

      - name: Checkout web4 (sibling protocol repo)
        uses: actions/checkout@v4
        with:
          repository: TeoSlayer/pilotprotocol
          path: web4

      - name: Checkout common
        uses: actions/checkout@v4
        with:
          repository: pilot-protocol/common
          path: common

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache: true
          cache-dependency-path: eventstream/go.sum

      - name: Run tests with coverage
        working-directory: eventstream
        run: go test -race -coverprofile=coverage.out -covermode=atomic ./...

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v6
        with:
          files: ./eventstream/coverage.out
          flags: unittests
          name: eventstream
          fail_ci_if_error: false

      - name: Coverage summary
        working-directory: eventstream
        run: go tool cover -func=coverage.out | tail -20

      - uses: actions/upload-artifact@v4
        with:
          name: coverage-eventstream
          path: eventstream/coverage.out
          retention-days: 30
